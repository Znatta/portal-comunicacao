spring.application.name=portal-comunicacao
spring.profiles.active=${SPRING_PROFILES_ACTIVE:local}

# Database & Pool (HikariCP)
spring.datasource.url=${SPRING_DATASOURCE_URL}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
spring.datasource.hikari.maximum-pool-size=4
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=20000

# Configurações de Upload
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# Supabase Storage
supabase.url=${SUPABASE_URL}
supabase.key=${SUPABASE_KEY}
supabase.bucket=noticias

# JWT
api.security.token.secret=${JWT_SECRET:my-secret-key}

# Configurações do Brevo (E-mail)
brevo.api_key=${BREVO_API_KEY}
brevo.from_email=${BREVO_FROM_EMAIL}
brevo.from_name=Insights Porto
