# Banco de Dados H2 (Em MemÃ³ria)
spring.datasource.url=jdbc:h2:mem:portaldb;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# Desabilitar Flyway para H2 (ou usar migrations compatÃ­veis)
spring.flyway.enabled=false
spring.jpa.hibernate.ddl-auto=create-drop

# Mock de Storage (Local se necessÃ¡rio, ou apenas logs)
supabase.bucket=test-bucket

# Mock de E-mail
brevo.api_key=mock-key
brevo.from_email=test@test.com
